---
interface Props {
  areas: string[];
}

const { areas } = Astro.props;
---

<div class="focus-area-list">
  {areas.map((area) => (
    <span class="focus-area-chip" tabindex="0">
      <span class="focus-area-chip__label">{area}</span>
    </span>
  ))}
</div>

<style>
  .focus-area-list {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    overflow: visible;
  }

  .focus-area-chip {
    position: relative;
    isolation: isolate;
    display: inline-flex;
    align-items: center;
    min-height: 28px;
    padding: 5px 10px;
    border-radius: 999px;
    border: 1px solid #111111;
    background: #ffffff;
    color: #111111;
    font-size: 0.78rem;
    line-height: 1.2;
    cursor: default;
    outline: none;
    overflow: visible;
    transition:
      border-color 0.2s ease,
      box-shadow 0.2s ease,
      transform 0.2s ease;
  }

  .focus-area-chip::before,
  .focus-area-chip::after {
    position: absolute;
    inset: -1px;
    border-radius: inherit;
    content: "";
    opacity: 0;
    pointer-events: none;
  }

  .focus-area-chip::before {
    z-index: -2;
    background:
      radial-gradient(circle at 20% 35%, rgba(152, 211, 255, 0.55), transparent 34%),
      radial-gradient(circle at 78% 62%, rgba(184, 230, 255, 0.48), transparent 38%),
      linear-gradient(135deg, rgba(233, 247, 255, 0.92), rgba(205, 235, 255, 0.78));
    transform: scale(0.96);
  }

  .focus-area-chip::after {
    z-index: -1;
    background:
      linear-gradient(95deg, transparent 0 24%, rgba(255, 255, 255, 0.66) 34%, transparent 48%),
      repeating-radial-gradient(
        ellipse at 50% 110%,
        rgba(96, 177, 232, 0.22) 0 2px,
        transparent 2px 10px
      );
    background-size: 220% 100%, 120% 120%;
    mix-blend-mode: screen;
  }

  .focus-area-chip__label {
    position: relative;
    z-index: 1;
  }

  .focus-area-chip:hover,
  .focus-area-chip:focus-visible {
    border-color: #6bb8ee;
    box-shadow: 0 8px 22px rgba(80, 160, 215, 0.18);
    transform: translateY(-1px);
  }

  .focus-area-chip:hover::before,
  .focus-area-chip:hover::after,
  .focus-area-chip:focus-visible::before,
  .focus-area-chip:focus-visible::after {
    opacity: 1;
  }

  .focus-area-chip:hover::before,
  .focus-area-chip:focus-visible::before {
    animation: focus-water-swell 2.2s ease-in-out infinite;
  }

  .focus-area-chip:hover::after,
  .focus-area-chip:focus-visible::after {
    animation: focus-water-glint 1.8s linear infinite;
  }

  @keyframes focus-water-swell {
    0%,
    100% {
      transform: scale(0.98) translateY(0);
      filter: hue-rotate(0deg);
    }

    45% {
      transform: scale(1.04) translateY(-1px);
      filter: hue-rotate(8deg);
    }

    70% {
      transform: scale(1.01) translateY(1px);
    }
  }

  @keyframes focus-water-glint {
    0% {
      background-position: 140% 0, 0 0;
    }

    100% {
      background-position: -80% 0, 28px -16px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .focus-area-chip:hover::before,
    .focus-area-chip:hover::after,
    .focus-area-chip:focus-visible::before,
    .focus-area-chip:focus-visible::after {
      animation: none;
    }
  }

</style>
